services:
  llama:
    image: the-clink-llama-cuda
    container_name: the-clink-llama
    init: true
    build:
      context: .
      dockerfile: llama-cuda.containerfile
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:9931/health"]
      interval: 3s
      start_period: 15s
      retries: 5
    restart: always
    devices:
      - "nvidia.com/gpu=all"
    command: [
      "/home/user/llama/llama-server", "--models-preset", "/home/user/.config/llama.cpp/config.ini",
    ]
    expose:
      - "9931"
